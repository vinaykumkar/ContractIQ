# ContractIQ ML dependencies (Phase 2+)
# Install with:  pip install -r ml/requirements.txt
# Versions reflect the environment the pipeline was developed and tested on.

torch==2.13.0
transformers==5.16.1
tokenizers==0.23.1
datasets==5.0.1
scikit-learn==1.9.0  # retrieval stage (TF-IDF block ranking)
psutil==7.2.2
numpy>=2.0

# pytest runs the ML tests:  pytest ml/tests -q
pytest==9.1.1
